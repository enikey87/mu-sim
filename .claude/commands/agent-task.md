---
description: Агент-исполнитель берёт одну задачу из очереди issues (метка task) и доводит её до мержа
argument-hint: <имя агента, например kimi | cursor | deepseek>
---

Ты — агент-исполнитель **$ARGUMENTS** в репозитории enikey87/mu-sim. Работаешь сам, спросить некого.
Ниже `<я>` — это `$ARGUMENTS`.

Если имя не передано (строка выше пустая) — остановись и напиши: «Запусти как `/agent-task <имя>`». Больше ничего не делай.

## 0. Правила

Прочитай `CLAUDE.md` и `README.md` целиком — они обязательны и важнее этой команды там, где с ней расходятся.

## 1. Сначала свои незаконченные PR

```sh
gh pr list --state open --label agent:<я>
```

- Ветка отстала от `main` или конфликт → `git fetch && git merge origin/main` (**не** rebase, **не** force-push), разреши конфликт, прогони `npm run typecheck`, `npm run lint`, `npx vitest run`, запушь.
- CI красный → почини в той же ветке.
- CI зелёный и ветка на текущем `main` → `gh pr merge <N> --merge`.

Есть незаконченный PR — доведи его и **закончи сессию**, новую задачу не бери.

## 2. Взять одну задачу

Кандидаты — открытые issues с меткой `task` и без метки `agent:*`:

```sh
gh issue list --state open --label task --json number,title,labels
```

Подходящий — тот, чьи зависимости закрыты. Прочитай тело кандидата (`gh issue view <N>`): строка вида «Зависит от #M: не брать, пока #M открыт» — это зависимость. Если хоть одна из них открыта (`gh issue view <M> --json state --jq .state` → `OPEN`), пропусти кандидата, даже если он первый в списке. Не берись «заодно» и за зависимость: её ведёт другой агент.

Возьми первый подходящий:

```sh
gh label create agent:<я> 2>/dev/null
gh issue edit <N> --add-label agent:<я>
```

Подожди 30 секунд и проверь, чья метка `agent:*` на этом issue появилась первой:

```sh
gh api repos/enikey87/mu-sim/issues/<N>/events \
  --jq '[.[] | select(.event=="labeled" and (.label.name|startswith("agent:")))][0].label.name'
```

- Первая — не твоя → `gh issue edit <N> --remove-label agent:<я>` и возьми следующий issue.
- Первая — твоя → комментарий в issue: `gh issue comment <N> --body "беру — <я>"`.

Подходящих задач нет — напиши «очередь пуста» и закончи.

## 3. Работа

- Ветка `agent/<я>/issue-<N>` от свежего `origin/main` — вместо имени ветки, указанного в issue.
- Цель — та, что описана в issue. Приёмка — аудит по смыслу после мержа, а не твои цифры.
- Коммиты по-английски (Conventional Commits), в конце тела — трейлер `Agent: <я>`.

## 4. PR

```sh
gh pr create --base main --label agent:<я>
```

В описании: `Closes #<N>`; что сделано; допущения (спросить некого — записать, а не выдумывать молча); негативные контроли (сломал → красный → вернул).

## 5. Мерж — сам

Когда CI зелёный и ветка на текущем `main` (иначе — шаг 1): `gh pr merge <N> --merge`.

## Нельзя

- трогать `.github/`, конфиги линтера и тестов, секцию `scripts` в `package.json`;
- удалять или ослаблять тесты, чтобы CI позеленел;
- пушить в `main`, трогать чужие ветки и issue с чужой меткой `agent:*`.

Три красных CI подряд на одной и той же проблеме → объясни в PR, что не получается, сними свою метку с issue (`gh issue edit <N> --remove-label agent:<я>`) и закончи сессию.

Одна задача за сессию.
