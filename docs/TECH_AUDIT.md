# Технический аудит

Первый проход: main @ 1d830db, 2026-09-21. **Актуализировано: main @ 3692551, 2026-09-23** — ссылки и метрики перепроверены на этой ревизии; закрытые пункты свёрнуты в таблицу (полные разборы — в истории git этого файла).

## Метод

Прочитан весь `src/` (engine, content, ui), конфиги, CI, инструменты. Прогнано на 3692551:

| Проверка | Результат |
|---|---|
| `npx vitest run` | 552 passed, 2 skipped (35 файлов, 1 пропущен) |
| `npm run test:rules` | покрытие engine/rules 100/100/100/100 — CI-гейт |
| `npm run rules:report` | не сработали 57: редкие 10 · детерминированные 22 · только в финалах 25 · необъяснённые 0 |
| `npm run build` | 722 kB JS (222 kB gzip), один чанк, warning >500 kB |
| `npm audit` | 0 уязвимостей |
| бандл `content/rules` + `node` | 284 правила в рантайме (литералы + фабричные: хор, квесты, сцены, финалы) |

Утверждения «прочитано в коде» и «наблюдалось в прогоне» разделены: где вывод сделан только из чтения, это сказано явно.

## Архитектура в двух абзацах

Чистый фронтенд: Vite 8 + React 19 + TS, без бэкенда, сохранение в localStorage. `src/engine/` — движок: изолированный модуль правил `engine/rules/` (событие + факты → самое специфичное правило; blackboard'ы мира и персонажей; отложенные события; приоритеты речи; CI-гейт покрытия 100%), классификатор свободного ввода `input.ts` (детерминированный, без ML), `game.ts` — оркестратор (1654 строки), `ui-state.ts` — эфемерное UI-состояние (#50). `src/content/` — тексты, 284 правила, реестр ключей памяти `memkeys.ts` (#49). `src/ui/` — React-телефон с ручной инкрементальной синхронизацией ленты. Инструменты: seeded playtest-бот с replay, отчёт покрытия правил, дебаг-панель `?debug`.

CI (`.github/workflows/pages.yml`): `npm ci` → `npm audit --audit-level=high` → typecheck → lint → vitest → `test:rules` (покрытие) → `rules:stable` → build → e2e против прод-сборки. Журналы дефектов — `docs/TECH_DEFECTS.md`, `docs/PLAYTEST_ISSUES.md`.

Серьёзность: **P1** — может сломать игру у игрока · **P2** — слепая зона / деградация · **P3** — гигиена.

---

## Открытые и принятые

### P3-3. Бандл 722 kB одним чанком (222 kB gzip) — принято, кроме варианта 2

Замерено `npm run build`. Почти всё — контент (тексты отмазок, сцен, сериалов). Для браузерной шутки приемлемо; на медленной сети ~1–2 с только JS. Warning >500 kB не глушить — он единственный сторож роста.

Остаётся развилка: dynamic import контента третьего акта (`payday`, `finales`) — реальная экономия, но контент синхронный (`game.ts` дёргает его напрямую), асинхронность пролезет в движок. `manualChunks` (react vendor) — косметика, gzip-итог тот же.

### P3-6. Две версии TypeScript — принято

`package.json`: typecheck — `typescript-7` (`npm:typescript@7.0.2`), ESLint — `typescript` = `@typescript/typescript6@6.0.2` (typescript-eslint пока не умеет TS 7; README это описывает). Два компилятора могут расходиться в диагностике; каждый бамп — двойной. Следствие: `npx tsc` вызывает TS 6, проверка типов — только `npm run typecheck`. Убрать alias, когда typescript-eslint поддержит TS 7 (issue-напоминания нет).

### P3-7. `game.ts` — 1654 строки, god object

Один класс держит: конвейер сообщений, аудио, батарею, сборку вариантов, сцены, сериалы, бухгалтерию лжи, концовки, idle-жизнь, эндгейм, персистентность. Первый кандидат на вынос сделан в #50, но только наполовину по смыслу: в `ui-state.ts` (29 строк) переехали **поля** (status, typing, toast, notif, moos, busy, dead, charging, unread, feel, trace, sheetOpen); **поведение** осталось в `Game` — `flash` (`game.ts:512`), `notify` (`:565`), `feelFor` (`:837`), wall-таймеры (`:104–105`). `game.ts` уменьшился на 20 строк.

- ✅ Не рефакторить «в лоб». При следующем касании — по одному модулю; следующий кандидат — батарея/зарядка (`S.battery`, `game.ts:151`, `:595–598`, `die`).
- Разовый сплит на `engine/{turns,scenes,arcs,lies,endings}.ts` — дорого, дифф-шум, выгода отложенная.

### P3-9 (остаток). Комментарий у `window.__alik`

`src/main.tsx:20` — `// для отладки из консоли`, но это API, через которое играет `scripts/playtest-tech.mjs` (`:18`, `:49`, `:77`). ✅ Одна строка: назвать потребителя, чтобы глобал не удалили как отладочный мусор. (Остальное из P3-9 — LICENSE, `.nvmrc`, `engines` — закрыто в #40.)

### P3-2. `loadState`: shallow merge без валидации типов — исключено решением оператора (2026-09-22)

Находка верна: `src/engine/state.ts:139` — `{ ...freshState(), ...s }`; вложенные объекты (`stats`, `rules`, `mem`) заменяются сохранением целиком, поле, добавленное внутрь вложенного объекта, у старого сохранения будет `undefined` (прецедент — `rules.said ??= {}`, `game.ts:132`); битые типы (`debt: "abc"`) протекают в игру. Сейчас спасает бамп `SAVE_KEY`. В работу не берётся.

### Ручная инкрементальная синхронизация ленты — принято

`syncMessageNodes` (`src/ui/Chat.tsx:28`), `MessageList` (`:76`): creation/sync O(Δ), родитель всё ещё видит до `LIVE_RENDER_CAP = 1000` детей (`:23`). Не трогать до следующего удара о frame time; тогда — виртуализация, а не третий слой ручного кэша (см. `TECH_DEFECTS.md`).

---

## Закрытые

| Пункт | Чем закрыт | Доказательство |
|---|---|---|
| **P1-1** софт-лок: `busy` не сбрасывался при ошибке | #40/#42 | `sendTurn` — `finally` сбрасывает `inPlayerTurn` и `ui.busy` (`game.ts:972`); `answerJob`/`onIdle` — `catch → recoverTurn` (`:1518`, `:1544`, определение `:216`) |
| **P2-1** e2e гонялись против dev-сервера | #44 | `playwright.config.ts:21` — `build && preview --strictPort`, `reuseExistingServer: false`; тест в `tests/e2e/ui.spec.ts` краснеет, если отвечает dev-сервер |
| **P2-2** нет ErrorBoundary | #40 | `src/ui/App.tsx:43` `class Crash`, `:45` `getDerivedStateFromError` |
| **P2-4** CI без audit и кэша браузеров | #44 наполовину | `npm audit --audit-level=high` сразу после `npm ci`. Кэш **замерен и отвергнут**: `playwright install` 22 с без кэша против 23 с с кэшем (попадание подтверждено), job 111 с против 112 с |
| **P3-1** lint-гейты ослаблены | #46 | `no-explicit-any` и `exhaustive-deps` — `error` (`eslint.config.js:26`, `:29`); исключения — точечные `eslint-disable-next-line` с причиной. Негативный контроль: намеренный `any` валит `npm run lint` |
| **P3-4** лента без live-region | #40 | `Chat.tsx:150` — `role="log" aria-live="polite"`; слот «новые сообщения» — `:162` |
| **P3-5** `android/` в дереве | отпал | каталога нет; хвост — `'android/**'` в ignore `eslint.config.js:9` |
| **P3-6** (часть) LICENSE, `.nvmrc`, `engines` | #40 | `LICENSE`, `.nvmrc` = `24`, `"engines": {"node": ">=24"}` |
| **P3-8** слепые зоны playtest-бота | #45 | классификация «почему не сработало» переехала в `coverage.ts` и печатается отчётом; бот получил шанс свободного ввода — без него классификатор ввода не имел покрытия вовсе. Диагноз находки («бот не мычит») не подтвердился |
| **P3-10** список `RARE` протух | #48 | `npm run rules:stable` (шаг CI) падает, если запись срабатывает во всех трёх выборках. Негативный контроль: возвращённый `Scene_lend` → падение ровно на нём. Сейчас в `src/tools/rare.ts` 12 записей (раунды 14–15 добавили `Chorus_garik_FedUp`, вычеркнули три) |
| **P2-3** горячий путь `facts()` | опровергнуто замером (2026-09-22) | 0.011–0.020 мс на вызов; за ход 0.43 мс на первом и 0.13 мс на 400-м; журнал обещаний к 431-му ходу — 25, а не «сотни». Мемоизация дала бы поверхность инвалидации без выгоды |
| **Реестр ключей памяти** (ARCHITECTURE §6.2) | #49 | `content/memkeys.ts`; линтер отвергает неизвестные ключи |

## Проверено — не проблема

| Подозрение | Чем проверено | Итог |
|---|---|---|
| XSS через ввод игрока | grep `dangerouslySetInnerHTML` — нет; ввод рендерится React-текстом; `PhotoSvg` статичен | чисто |
| ReDoS в классификаторе ввода | чтение `input.ts`: регэкспы без вложенных квантификаторов, `spelled()` строит линейный паттерн | чисто |
| Утечка таймеров | `dispose()` чистит `timerIds`, wall-таймеры, `sleepWaiters`, аудио (`game.ts:240–247`); тест-сторож `pendingTimers()` (`:193`) | чисто |
| Коллизии хеша анти-повтора (`Seen`, djb2 32-bit) | чтение `uniq.ts`: коллизия лишь перегенерирует реплику | безвредно |
| Переполнение localStorage | `saveState` в try/catch, сообщения режутся до 150 (`state.ts:149`) | чисто |
| Рост DOM ленты | `LIVE_RENDER_CAP = 1000` (`Chat.tsx:23`) | ограничен |
| Дубли имён правил | конструктор `RuleSet` бросает (`ruleset.ts:50`) | гейт есть |
| Вечно проигрывающие правила (shadowed) | `lintRules` — статически, в тестах | гейт есть |
| Уязвимости зависимостей | `npm audit` в CI | 0 |

## Метрики (3692551)

| Метрика | Значение |
|---|---|
| Строк кода `src/` без тестов | 9 846 |
| Строк тестов | 5 911 unit + 219 e2e (0,60 к коду без e2e) |
| Тесты vitest | 552 passed / 2 skipped, 35 файлов |
| Покрытие `engine/rules` | 100% stmts/branch/func/lines (CI-гейт) |
| Правил игры | 284 (рантайм) |
| Бандл | 722 kB / 222 kB gzip, один чанк |
| e2e | desktop + mobile Chromium против прод-сборки |

## Что делать первым

1. **P3-9** — одна строка комментария у `window.__alik`.
2. **P3-7** — при следующем касании вынести батарею/зарядку из `game.ts`.
3. **P3-3**, вариант 2 — ленивая загрузка третьего акта: единственный оставшийся пункт про игрока, ценой асинхронности в движке.
